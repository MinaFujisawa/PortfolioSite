$(document).ready(function () {	//Load common parts	$(function() {       $("#header").load("/portfolio/html/header.html");       $("#footer").load("/portfolio/html/footer.html");  	});	// ---- for full screen	hsize = $(window).height();	var windowsize = $(window).width();    // if(windowsize < 480){    // 	$(".nav_menu").addClass('js_full_screen');    // }    $('.js_full_screen').css('height', hsize + 'px');    $(window).resize(function () {    	hsize = $(window).height();    	$('.js_full_screen').css('height', hsize + 'px');    });    //Hover for PC    $('button, a:not(.thumbnail)').hover(function(event) {     	$(this).stop().animate({'opacity': 0.5}, 1500, 'easeOutQuint');    },function() {     	$(this).stop().animate({'opacity': 1} , 1500, 'easeOutQuint');    });    //Hover for mobile	$('button, a:not(.thumbnail)')	  .on('touchstart', function(){	    $(this).stop().animate({'opacity': 0.5}, 500);	}).on('touchend', function(){	    $(this).stop().animate({'opacity': 1} , 500);	});		// // ---- for toggle menu Fumin	// function slideMenu() {	// 	$('#btn_toggle').toggleClass('js_on');	// 	$('.nav_menu').toggleClass('active');	// 	if($('.nav_menu').hasClass('active')){	// 		$('.nav_menu').toggleClass('js_visible');	// 	}	// 	$('.nav_menu').animate({	// 		top: $('.nav_menu').hasClass('active') ? '0%' : '-100%'	// 	}, 800, function(){	// 		if(!$('.nav_menu').hasClass('active')){	// 			$('.nav_menu').toggleClass('js_visible');	// 		}	// 	});	// }	// var activeState = $('.nav_menu').hasClass('active');	// if(activeState){	// 	$(document).on('click touchend', function(event) {	// 		slideMenu();	// 	});	// } else {	// 	$('#btn_toggle').click(function(event) {	// 	    slideMenu();	// 	});	// }		// function slideMenu() {	// 	var activeState = $('.nav_menu').hasClass('active');	// 	$('.nav_menu').animate({	// 		top: activeState ? '0%' : '-100%'	// 	}, 600);	// 	if(activeState) {	// 		$('.nav_menu').addClass('js_visible');	// 	}	// }	// $('#btn_toggle').click(function(event) {	// 	event.stopPropagation();	// 	event.preventDefault();	// 	$('#btn_toggle').stop(true, false).toggleClass('js_on');	// 	$('.nav_menu').stop(true, false).toggleClass('active');	// 	$('.nav_menu').stop(true, false).toggleClass('js_visible');	// 	slideMenu();	// 	console.log('toggle done');	// });	// $('.nav_menu ul li a').click(function(){	// 	$('.nav_menu').removeClass('active js_visible');	// 	$('#btn_toggle').removeClass('js_on');	// 	slideMenu();	// });	// // close when clicked other storageArea	// // something wrong on mobile	// $(document).on('click touchend', function(event) {	//   // if (!$(event.target).closest('.nav_menu').length) { //  	// for only pc visiter //  	if ($(window).width() > 768) { //  		$('.nav_menu').removeClass('active js_visible'); //  		$('#btn_toggle').removeClass('js_on'); //  		slideMenu(); //  		console.log('remove done');	   //  	}	//   // }	// });	// ---- for smooth scrolling	$.easing.def = 'easeOutQuart';	$('a[href*="#"]:not([href="#"])').click(function() {		if (location.pathname.replace(/^\//,'') == this.pathname.replace(/^\//,'') && location.hostname == this.hostname) {			var target = $(this.hash);			target = target.length ? target : $('[name=' + this.hash.slice(1) +']');			if (target.length) {				$('html, body').animate({					scrollTop: target.offset().top,				}, 1000);				return false;			}		}	});	// ---- for scroll reveal	window.sr = ScrollReveal({ reset: false });	sr.reveal('.js_scroll', { duration: 1000, distance: '10px' });		var touch = 'ontouchstart' in document.documentElement	|| navigator.maxTouchPoints > 0	|| navigator.msMaxTouchPoints > 0;	if (touch) { // remove all :hover stylesheets		if ('#btn_toggle'.match(':hover')) {			$('#btn_toggle').css('opacity','1');		}	}});